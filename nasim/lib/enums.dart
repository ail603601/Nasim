enum MenuType { Overview, Controll, DeviceInformation, Licenses }
enum DeviceAccessibility { InAccessible, AccessibleLocal, AccessibleInternet }

enum ServerRequest { get_device_data_if_presernt, send_wishlist }
